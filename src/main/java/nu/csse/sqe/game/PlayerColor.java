package nu.csse.sqe.game;

/**
 * The six player colors used in Risk (design: one color per seat, in fixed order).
 */
public enum PlayerColor {
    RED,
    BLUE,
    GREEN,
    ORANGE,
    PINK,
    CYAN
}
